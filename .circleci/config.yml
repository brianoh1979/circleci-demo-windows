version: 2.1

#Comment

orbs:
  win: circleci/windows@1.0.0

jobs:
  build:
    machine: true   
    resource_class: brianoh1979/brian-winrunner
    steps:
      - checkout
      - run: docker build -t dotnetapp .
      - run: docker ps -a 
      - run: 
          name: "Docker daemon test"
          shell: powershell.exe
          command: |
            iwr -useb http://localhost:8080
